# Help Center migration notes

## Goal

Move the current support documentation into a GitBook `Help Center` space.

## Current blockers

- `help-center/overview.mdx` uses `Note`

## Conversion approach

1. Convert the note block into a standard callout or blockquote.
2. Keep the trust and safety page and media assets page as separate docs.

